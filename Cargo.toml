[package]
name = "ambition_inventory_ui"
version = "0.1.0"
edition = "2021"
description = "Standalone Bevy prototype for a reusable inventory UI interaction model."
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
bevy = "0.18.1"
bevy_lunex = { path = "vendor/bevy_lunex/crate" }
